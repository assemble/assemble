<ul class="nav">
{{#collection "categories"}}
  <li><span{{#is ../category this.category}} class="active"{{/is}}><a href="{{link-to (first items)}}" class="nav-section-title">{{inflection}}</a></span>
    <ul class="nav nav-pills nav-stacked {{#is ../category this.category}} active{{/is}}">
    {{#each items}}
      {{#is this.data.draft true}}{{else}}
      {{> sidenav-link ..}}
      {{/is}}
    {{/each}}
    </ul>
  </li>
{{/collection}}
</ul>
