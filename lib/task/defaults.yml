###
# ASSEMBLE DEFAULTS
# These defaults may be overridden in task or target options
####

# Extension Map
extensions:
  # Handlebars extensions
  handlebars:
  - '.hbs'
  - '.mustache'
  - '.template'
  - '.tmpl'
  - '.tpl'

  # YAML
  yaml:
  - '.yml'
  - '.yaml'
  - 'DOTFILE' # Some file that is YAML formatted, but no '.' or yaml indication